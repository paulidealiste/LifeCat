// Generated by: gen
// TypeWriter: slice
// Directive: +gen on LifeCatOTU

package lifecatsink

// Sort implementation is a modification of http://golang.org/pkg/sort/#Sort
// Copyright 2009 The Go Authors. All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found at http://golang.org/LICENSE.

// LifeCatOTUSlice is a slice of type LifeCatOTU. Use it where you would use []LifeCatOTU.
type LifeCatOTUSlice []LifeCatOTU

// Where returns a new LifeCatOTUSlice whose elements return true for func. See: http://clipperhouse.github.io/gen/#Where
func (rcv LifeCatOTUSlice) Where(fn func(LifeCatOTU) bool) (result LifeCatOTUSlice) {
	for _, v := range rcv {
		if fn(v) {
			result = append(result, v)
		}
	}
	return result
}

// SortBy returns a new ordered LifeCatOTUSlice, determined by a func defining ‘less’. See: http://clipperhouse.github.io/gen/#SortBy
func (rcv LifeCatOTUSlice) SortBy(less func(LifeCatOTU, LifeCatOTU) bool) LifeCatOTUSlice {
	result := make(LifeCatOTUSlice, len(rcv))
	copy(result, rcv)
	// Switch to heapsort if depth of 2*ceil(lg(n+1)) is reached.
	n := len(result)
	maxDepth := 0
	for i := n; i > 0; i >>= 1 {
		maxDepth++
	}
	maxDepth *= 2
	quickSortLifeCatOTUSlice(result, less, 0, n, maxDepth)
	return result
}

// GroupByString groups elements into a map keyed by string. See: http://clipperhouse.github.io/gen/#GroupBy
func (rcv LifeCatOTUSlice) GroupByString(fn func(LifeCatOTU) string) map[string]LifeCatOTUSlice {
	result := make(map[string]LifeCatOTUSlice)
	for _, v := range rcv {
		key := fn(v)
		result[key] = append(result[key], v)
	}
	return result
}

// Sort implementation based on http://golang.org/pkg/sort/#Sort, see top of this file

func swapLifeCatOTUSlice(rcv LifeCatOTUSlice, a, b int) {
	rcv[a], rcv[b] = rcv[b], rcv[a]
}

// Insertion sort
func insertionSortLifeCatOTUSlice(rcv LifeCatOTUSlice, less func(LifeCatOTU, LifeCatOTU) bool, a, b int) {
	for i := a + 1; i < b; i++ {
		for j := i; j > a && less(rcv[j], rcv[j-1]); j-- {
			swapLifeCatOTUSlice(rcv, j, j-1)
		}
	}
}

// siftDown implements the heap property on rcv[lo, hi).
// first is an offset into the array where the root of the heap lies.
func siftDownLifeCatOTUSlice(rcv LifeCatOTUSlice, less func(LifeCatOTU, LifeCatOTU) bool, lo, hi, first int) {
	root := lo
	for {
		child := 2*root + 1
		if child >= hi {
			break
		}
		if child+1 < hi && less(rcv[first+child], rcv[first+child+1]) {
			child++
		}
		if !less(rcv[first+root], rcv[first+child]) {
			return
		}
		swapLifeCatOTUSlice(rcv, first+root, first+child)
		root = child
	}
}

func heapSortLifeCatOTUSlice(rcv LifeCatOTUSlice, less func(LifeCatOTU, LifeCatOTU) bool, a, b int) {
	first := a
	lo := 0
	hi := b - a

	// Build heap with greatest element at top.
	for i := (hi - 1) / 2; i >= 0; i-- {
		siftDownLifeCatOTUSlice(rcv, less, i, hi, first)
	}

	// Pop elements, largest first, into end of rcv.
	for i := hi - 1; i >= 0; i-- {
		swapLifeCatOTUSlice(rcv, first, first+i)
		siftDownLifeCatOTUSlice(rcv, less, lo, i, first)
	}
}

// Quicksort, following Bentley and McIlroy,
// Engineering a Sort Function, SP&E November 1993.

// medianOfThree moves the median of the three values rcv[a], rcv[b], rcv[c] into rcv[a].
func medianOfThreeLifeCatOTUSlice(rcv LifeCatOTUSlice, less func(LifeCatOTU, LifeCatOTU) bool, a, b, c int) {
	m0 := b
	m1 := a
	m2 := c
	// bubble sort on 3 elements
	if less(rcv[m1], rcv[m0]) {
		swapLifeCatOTUSlice(rcv, m1, m0)
	}
	if less(rcv[m2], rcv[m1]) {
		swapLifeCatOTUSlice(rcv, m2, m1)
	}
	if less(rcv[m1], rcv[m0]) {
		swapLifeCatOTUSlice(rcv, m1, m0)
	}
	// now rcv[m0] <= rcv[m1] <= rcv[m2]
}

func swapRangeLifeCatOTUSlice(rcv LifeCatOTUSlice, a, b, n int) {
	for i := 0; i < n; i++ {
		swapLifeCatOTUSlice(rcv, a+i, b+i)
	}
}

func doPivotLifeCatOTUSlice(rcv LifeCatOTUSlice, less func(LifeCatOTU, LifeCatOTU) bool, lo, hi int) (midlo, midhi int) {
	m := lo + (hi-lo)/2 // Written like this to avoid integer overflow.
	if hi-lo > 40 {
		// Tukey's Ninther, median of three medians of three.
		s := (hi - lo) / 8
		medianOfThreeLifeCatOTUSlice(rcv, less, lo, lo+s, lo+2*s)
		medianOfThreeLifeCatOTUSlice(rcv, less, m, m-s, m+s)
		medianOfThreeLifeCatOTUSlice(rcv, less, hi-1, hi-1-s, hi-1-2*s)
	}
	medianOfThreeLifeCatOTUSlice(rcv, less, lo, m, hi-1)

	// Invariants are:
	//	rcv[lo] = pivot (set up by ChoosePivot)
	//	rcv[lo <= i < a] = pivot
	//	rcv[a <= i < b] < pivot
	//	rcv[b <= i < c] is unexamined
	//	rcv[c <= i < d] > pivot
	//	rcv[d <= i < hi] = pivot
	//
	// Once b meets c, can swap the "= pivot" sections
	// into the middle of the slice.
	pivot := lo
	a, b, c, d := lo+1, lo+1, hi, hi
	for {
		for b < c {
			if less(rcv[b], rcv[pivot]) { // rcv[b] < pivot
				b++
			} else if !less(rcv[pivot], rcv[b]) { // rcv[b] = pivot
				swapLifeCatOTUSlice(rcv, a, b)
				a++
				b++
			} else {
				break
			}
		}
		for b < c {
			if less(rcv[pivot], rcv[c-1]) { // rcv[c-1] > pivot
				c--
			} else if !less(rcv[c-1], rcv[pivot]) { // rcv[c-1] = pivot
				swapLifeCatOTUSlice(rcv, c-1, d-1)
				c--
				d--
			} else {
				break
			}
		}
		if b >= c {
			break
		}
		// rcv[b] > pivot; rcv[c-1] < pivot
		swapLifeCatOTUSlice(rcv, b, c-1)
		b++
		c--
	}

	min := func(a, b int) int {
		if a < b {
			return a
		}
		return b
	}

	n := min(b-a, a-lo)
	swapRangeLifeCatOTUSlice(rcv, lo, b-n, n)

	n = min(hi-d, d-c)
	swapRangeLifeCatOTUSlice(rcv, c, hi-n, n)

	return lo + b - a, hi - (d - c)
}

func quickSortLifeCatOTUSlice(rcv LifeCatOTUSlice, less func(LifeCatOTU, LifeCatOTU) bool, a, b, maxDepth int) {
	for b-a > 7 {
		if maxDepth == 0 {
			heapSortLifeCatOTUSlice(rcv, less, a, b)
			return
		}
		maxDepth--
		mlo, mhi := doPivotLifeCatOTUSlice(rcv, less, a, b)
		// Avoiding recursion on the larger subproblem guarantees
		// a stack depth of at most lg(b-a).
		if mlo-a < b-mhi {
			quickSortLifeCatOTUSlice(rcv, less, a, mlo, maxDepth)
			a = mhi // i.e., quickSortLifeCatOTUSlice(rcv, mhi, b)
		} else {
			quickSortLifeCatOTUSlice(rcv, less, mhi, b, maxDepth)
			b = mlo // i.e., quickSortLifeCatOTUSlice(rcv, a, mlo)
		}
	}
	if b-a > 1 {
		insertionSortLifeCatOTUSlice(rcv, less, a, b)
	}
}
